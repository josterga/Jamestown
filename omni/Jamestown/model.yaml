# ai_context: when the prompt references the current user like 'I' or 'my', use
#   users.your_email to filter based on their email
fiscal_month_offset: 2

access_grants:
  embed_grant:
    user_attribute: region
    allowed_values: [ California ]

# dynamic_schemas:
#   d_schem:
#     from_schema: private
#     user_attribute: email

#@
#a change
#another change
ai_chat_topics: [ order_items, users ]

#comment
skills:
  a_skill:
    label: a skill
    description: a skill
    # required_access_grants: [ embed_grant ]
